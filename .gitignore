frontend/node_modules/
frontend/build/
frontend/.svelte-kit/

dist/
*.exe

# Local issue tracker (see docs/agents/issue-tracker.md)
.scratch/

# Generated UI assets (synced by `make build`). `.gitkeep` keeps `go:embed all:static`
# compiling on a bare go build; the server then serves `backend/placeholder.html`.
backend/static/*
!backend/static/.gitkeep
